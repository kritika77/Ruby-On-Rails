<% @page_title = "Delete Page" %>

<%= link_to("<< Back to list", {:action => 'index', :subject_id => @subject_id}, :class => 'back-link') %>

<div class="pages delete">
   <h2>Delete Pages</h2>

   <%= form_for(:subject, :url => {:action => 'destroy', :id => @page.id, :subject_id => @subject.id}) do |f| %>

    <p>Are you sure you want to permanently delete this page?</p>

    <p class="reference-name"><%= @page.name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Page") %>
    </div>

   <% end %>
 </div>
